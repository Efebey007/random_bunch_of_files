name: Disk Test

on:
  workflow_dispatch:
jobs:
 build:
    runs-on: ubuntu-latest
    steps:
      - name: Show disks & Make other disk ext4
        run: |
         df -h
         sudo mkfs.ext4 /dev/sdb

      - name: Mount the fs to /mnt
        run: |
         sudo mount /dev/sdb /mnt
